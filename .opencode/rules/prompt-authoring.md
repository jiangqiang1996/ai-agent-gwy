# 提示词编写规则

- 共享流程、考试上下文、输出格式等通用规则放在 `.opencode/rules/`，不要复制到每个 agent 文件。
- agent 文件只保留角色定位、专业能力、教学风格和该角色独有的答题/出题偏好。
- 文件命名、角色结构和回答长度遵循 `AGENTS.md`。
- 如果运行时契约变更，优先更新共享规则和 `orchestrator.md`，再检查各角色文件是否仍有局部特例需要保留。
